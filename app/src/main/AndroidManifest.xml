<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="andbook.example.protect_me_release">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon96"
        android:label="나를지켜줘"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".PermissionCheckAcitivty"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".TestallsosActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".TestcallAcitivity"
            android:theme="@style/AppTheme" />

        <service
            android:name="ModelFile.Scrum_Controll"
            android:enabled="true"
            android:exported="false"> <!-- 컴포넌트 취약점 제거 -->
        </service>
        <service
            android:name="ModelFile.Scrum_NoControll"
            android:enabled="true"
            android:exported="false"> <!-- 컴포넌트 취약점 제거 -->
        </service>
        <service
            android:name="ModelFile.Audio"
            android:enabled="true"
            android:exported="false"> <!-- 컴포넌트 취약점 제거 -->
        </service>
        <service
            android:name="ModelFile.TestScrum"
            android:enabled="true"
            android:exported="false"> <!-- 컴포넌트 취약점 제거 -->
        </service>
        <service
            android:name="ModelFile.TestAudio"
            android:enabled="true"
            android:exported="false"> <!-- 컴포넌트 취약점 제거 -->
        </service>

        <receiver
            android:name="AlarmFile.AlarmReceiver"
            android:enabled="true"
            android:exported="false"></receiver>
        <receiver
            android:name="AlarmFile.BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
